put all settings in settings.json (menu, theme)

change menu settings (remove football)

add more sites (tabs, tables, ....)

select text color on theming

add some actions in customizing and footer


0

I solved this by adding an exception to the anti-virus monitoring software (F-Secure). It removed the wait of several minutes before running.